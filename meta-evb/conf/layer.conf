# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

BBFILE_COLLECTIONS += "evb"
BBFILE_PATTERN_evb = ""

# should set compat to prevent bb warnings
LAYERSERIES_COMPAT_evb = "honister kirkstone"